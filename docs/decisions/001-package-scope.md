# ADR 001: Package scope

Status: accepted

The package owns repeatable AutoHotkey v2 build orchestration: tool acquisition, source preparation, compilation, portable archives, optional WiX invocation, and release publication. Consumer-specific application code, icon design, installer authoring, and runtime dependencies remain in consumer repositories.
